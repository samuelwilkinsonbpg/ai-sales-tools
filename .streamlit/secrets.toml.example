# Local dev only: copy to .streamlit/secrets.toml (gitignored) and fill in.
# On Streamlit Community Cloud, set this under your app's Settings -> Secrets instead —
# never commit a real key.
ANTHROPIC_API_KEY = "sk-ant-..."
